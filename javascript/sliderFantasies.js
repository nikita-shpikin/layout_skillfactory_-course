const items = document.querySelectorAll('.fantasies-content__item');
const images = document.querySelectorAll('.fantasies-images__item');



for (let item of items) {
  item.addEventListener('click', () => {
    console.log('x');
  })
}